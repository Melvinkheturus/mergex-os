import { ClientLaunchPage } from "./client-launch-client";

export const metadata = {
  title: "Client Launch | MergeX Sales OS",
  description: "Manage client onboarding and delivery transitions",
};

export default function Page() {
  return <ClientLaunchPage />;
}
